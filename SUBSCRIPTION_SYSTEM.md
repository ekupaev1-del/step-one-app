# Полная система подписок Step One

## ✅ Реализовано

### 1. Модель подписки
- **Триал**: 3 дня бесплатно
- **Цена после триала**: 199 ₽ в месяц
- **Автоплатеж**: Обязателен (Recurring)
- **Привязка карты**: Перед триалом (платеж 1 ₽)
- **НДС**: Не применяется (self-employed, УСН доходы)

### 2. Логика платежей (Robokassa)

#### Первый платеж (1 RUB)
- **Сумма**: 1 RUB
- **Параметр**: `Recurring=true` (обязательно!)
- **Назначение**: Привязка карты и создание parent invoice
- **Receipt**: Отправляется для фискализации
  - `sno: "usn_income"` (УСН доходы)
  - `payment_object: "service"`
  - `payment_method: "full_payment"`
  - `tax: "none"`
  - `name: "Step One subscription (1 month)"`

#### После успешного платежа 1 RUB:
- `subscription_status` = `"trial"`
- `trial_end_at` = `now() + 3 дня`
- `robokassa_parent_invoice_id` = InvoiceID (сохраняется для рекуррентных платежей)
- Пользователь получает **полный доступ** на 3 дня

#### После окончания триала (автоматически):
- Сервер проверяет истекшие триалы каждые 6 часов
- Инициируется рекуррентный платеж:
  - URL: `https://auth.robokassa.ru/Merchant/Recurring`
  - `PreviousInvoiceID` = `robokassa_parent_invoice_id`
  - `OutSum` = 199
  - Генерируется новый `InvoiceID`
  - Отправляется Receipt для фискализации

#### После успешного платежа 199 RUB:
- `subscription_status` = `"active"`
- `paid_until` = `now() + 30 дней`
- Пользователь получает доступ на 30 дней

#### При неудачном платеже:
- `subscription_status` = `"payment_failed"`
- Пользователь получает уведомление в боте
- Доступ к функциям блокируется

### 3. Telegram Mini App

#### Страница оплаты (`/payment`)
- Показывает статус подписки:
  - Триал активен (с датой окончания)
  - Подписка активна
  - Кнопка "Начать триал (1 ₽)" для новых пользователей
- При нажатии создается платеж 1 RUB с Recurring=true
- Редирект на страницу оплаты Robokassa

### 4. Бот

#### Проверка подписки
- Функция `checkSubscription()` проверяет:
  - `subscription_status`
  - `trial_end_at` (для триала)
  - `paid_until` (для активной подписки)
- Блокирует функции при отсутствии доступа

#### Сообщения
- При отсутствии доступа:
  ```
  ❌ Нет доступа
  
  Для использования функций бота необходимо активировать триал.
  
  💳 Подписка Step One
  После триала: 199 ₽ в месяц
  
  🎁 Триал бесплатный на 3 дня
  Для активации нужно привязать карту (списание 1 ₽)
  ```
- После активации триала:
  ```
  🎉 Триал активирован! У вас есть 3 дня полного доступа ко всем функциям бота.
  ```
- При ошибке оплаты:
  ```
  ❌ Ошибка оплаты. Триал закончился, но автоматическое списание не прошло.
  Пожалуйста, проверьте карту или обратитесь в поддержку.
  ```

### 5. База данных

#### Миграция: `migrations/update_subscription_system.sql`
- Добавлено поле `paid_until` (TIMESTAMPTZ)
- Обновлен enum `subscription_status`:
  - `'none'` - подписка не активирована
  - `'trial'` - триал активен
  - `'active'` - подписка активна
  - `'expired'` - подписка истекла
  - `'payment_failed'` - ошибка оплаты
- Добавлены индексы для оптимизации запросов

#### Таблица `users`:
- `subscription_status` - статус подписки
- `trial_end_at` - дата окончания триала
- `paid_until` - дата до которой оплачена подписка
- `robokassa_parent_invoice_id` - ID родительского платежа (для рекуррентных)
- `last_payment_status` - статус последнего платежа

#### Таблица `payments`:
- `invoice_id` - уникальный ID платежа
- `previous_invoice_id` - ID предыдущего платежа (для рекуррентных)
- `amount` - сумма
- `status` - статус (pending, success, fail, sent)
- `is_recurring` - флаг рекуррентного платежа

### 6. Cron / Background Job

#### `bot/src/services/recurringBilling.ts`
- **Функция**: `runRecurringBilling()`
- **Проверяет**: Пользователей с `subscription_status = 'trial'` и `trial_end_at < now()`
- **Действие**: Инициирует рекуррентный платеж 199 RUB
- **Запуск**: Каждые 6 часов + при старте бота

#### `renewActiveSubscriptions()`
- **Проверяет**: Активные подписки, которые заканчиваются завтра
- **Действие**: Продлевает подписку (списание 199 RUB)

### 7. API Endpoints

#### `POST /api/robokassa/create`
- Создает платеж 1 RUB с Recurring=true
- Параметры:
  - `userId` (number) - обязателен
- Возвращает:
  - `paymentUrl` - URL для редиректа на Robokassa
  - `invoiceId` - ID созданного платежа

#### `POST/GET /api/robokassa/result`
- Обрабатывает результат платежа от Robokassa
- Проверяет подпись с Password2
- Обрабатывает:
  - Платеж 1 RUB → активирует триал
  - Платеж 199 RUB → активирует подписку на 30 дней
- Отправляет уведомления в бот

#### `POST /api/notify-bot`
- Отправляет сообщения пользователю в Telegram
- Параметры:
  - `userId` (number) - обязателен
  - `message` (string) - опционально, кастомное сообщение

## 🔄 Как работает flow

### Активация триала:
1. Пользователь нажимает "Начать триал (1 ₽)" в Mini App
2. Создается платеж 1 RUB с Recurring=true
3. Пользователь переходит на страницу Robokassa
4. Привязывает карту (списание 1 RUB)
5. Robokassa отправляет уведомление на `/api/robokassa/result`
6. Система активирует триал на 3 дня
7. Пользователь получает уведомление в боте
8. Доступ к функциям открыт на 3 дня

### Автоматическое списание после триала:
1. Cron job проверяет истекшие триалы (каждые 6 часов)
2. Для каждого истекшего триала:
   - Используется `robokassa_parent_invoice_id`
   - Отправляется запрос на `https://auth.robokassa.ru/Merchant/Recurring`
   - Сумма: 199 RUB
   - Генерируется новый InvoiceID
3. Robokassa списывает 199 RUB с привязанной карты
4. Robokassa отправляет уведомление на `/api/robokassa/result`
5. Система активирует подписку на 30 дней
6. Пользователь получает уведомление в боте

### Продление активной подписки:
1. Cron job проверяет подписки, которые заканчиваются завтра
2. Инициируется рекуррентный платеж 199 RUB
3. После успешного платежа подписка продлевается на 30 дней

## 📋 Что нужно сделать вручную

### 1. Выполнить миграцию БД
```sql
-- Запустите файл migrations/update_subscription_system.sql в Supabase
```

### 2. Настроить переменные окружения в Vercel
- `ROBOKASSA_MERCHANT_LOGIN` - логин мерчанта
- `ROBOKASSA_PASSWORD1` - Password #1 (для создания платежей)
- `ROBOKASSA_PASSWORD2` - Password #2 (для проверки результатов)
- `TELEGRAM_BOT_TOKEN` - токен бота (для уведомлений)
- `MINIAPP_BASE_URL` - URL Mini App (опционально)

### 3. Настроить Robokassa
- **Result URL**: `https://ваш-домен.vercel.app/api/robokassa/result`
- **Success URL**: `https://ваш-домен.vercel.app/api/robokassa/success`
- **Метод**: POST или GET (оба поддерживаются)
- **Важно**: Robokassa должен передавать параметр `Shp_userId` в Result URL

## ✅ Проверка работы

1. **Триал активируется** после платежа 1 RUB
2. **Автоматическое списание** происходит после окончания триала
3. **Бот отправляет уведомления** при активации триала и продлении подписки
4. **Mini App показывает** правильный статус подписки
5. **Доступ блокируется** при отсутствии активной подписки/триала

## 🔍 Логирование

Все операции логируются:
- `[robokassa/create]` - создание платежа
- `[robokassa/result]` - обработка результата
- `[recurring]` - рекуррентные платежи
- `[checkSubscription]` - проверка подписки

## 📝 Важные замечания

1. **Parent Invoice ID** должен сохраняться при первом платеже 1 RUB
2. **Recurring=true** обязателен для первого платежа
3. **Receipt** отправляется для фискализации (УСН доходы, без НДС)
4. **Cron job** запускается автоматически при старте бота
5. **Уведомления** отправляются через `/api/notify-bot`
