# 🔄 Workflow деплоя: dev → main → production

## Проблема, которая была исправлена

**Проблема:** Бот использует production URL (`https://step-one-app.vercel.app`), который обновляется только из ветки `main`. Изменения в `dev` деплоятся в preview, который бот не видит.

**Решение:** После каждого пуша в `dev` автоматически мержить изменения в `main`, чтобы production обновлялся.

## Текущая настройка

- **Production Branch в Vercel:** `main`
- **Preview Branch в Vercel:** `dev` (автоматически)
- **Production URL:** `https://step-one-app.vercel.app`
- **Бот использует:** Production URL

## Автоматический workflow

### Вариант 1: Использовать скрипт (рекомендуется)

После каждого пуша в `dev`:

```bash
./merge-dev-to-main.sh
```

Этот скрипт:
1. Переключается на `main`
2. Мержит `dev` в `main`
3. Пушит в `main` (запускает production деплой)
4. Возвращается в `dev`

### Вариант 2: Ручной мерж

```bash
git checkout main
git merge dev --no-edit
git push origin main
git checkout dev
```

## Важно

⚠️ **После каждого пуша в `dev` нужно мержить в `main`**, чтобы бот видел изменения!

## Проверка деплоя

После мержа в `main`:
1. Зайдите в Vercel Dashboard → Deployments
2. Найдите последний деплой из `main`
3. Убедитесь, что статус "Ready"
4. Проверьте URL: `https://step-one-app.vercel.app`

## Альтернативное решение (для будущего)

Если хотите использовать preview URL для dev:
1. Настройте стабильный preview домен в Vercel
2. Обновите `MINIAPP_BASE_URL` в боте на preview URL
3. Тогда можно работать только в `dev` без мержа в `main`
