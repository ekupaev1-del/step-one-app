# 🧪 Настройка тестового миниапа - ПОШАГОВАЯ ИНСТРУКЦИЯ

## ✅ ЧТО НУЖНО СДЕЛАТЬ:

### 1. Инициализировать Git (если еще не сделано)

```bash
cd /Users/eminkupaev/Desktop/step-one-app
git init
git add .
git commit -m "Initial commit"
```

### 2. Создать GitHub репозиторий и подключить

```bash
# Создай репозиторий на GitHub (например, step-one-app)
# Затем подключи:
git remote add origin https://github.com/ТВОЙ_USERNAME/step-one-app.git
git branch -M main
git push -u origin main
```

### 3. Создать ветку dev для тестирования

```bash
git checkout -b dev
git push -u origin dev
```

### 4. Настроить Vercel для тестового миниапа

#### В Vercel Dashboard:

1. **Зайди в проект** → Settings → Git
2. **Production Branch**: установи `main` (продакшн)
3. **Preview Branches**: включи `dev` (тест)

#### Добавить переменные окружения для Preview:

Settings → Environment Variables → Add New

**Выбери окружение: Preview** и добавь:

```
NEXT_PUBLIC_SUPABASE_URL=https://ppisnuivnswwpkoxwpef.supabase.co
SUPABASE_SERVICE_ROLE_KEY=твой-сервисный-ключ-из-supabase
TELEGRAM_BOT_TOKEN=8528023493:AAGzAHYtRXW5OP38AjjrItzH-Idndm4hJ3A
```

**Важно:** Выбери именно **Preview** окружение, чтобы эти переменные использовались только для тестовых деплоев!

### 5. Настроить Root Directory в Vercel

Settings → General → Root Directory: `miniapp`

Или в настройках проекта укажи:
- Framework Preset: `Next.js`
- Root Directory: `miniapp`
- Build Command: `npm run build` (или оставь по умолчанию)
- Output Directory: `.next`

### 6. Задеплоить тестовую версию

```bash
# Убедись что ты в ветке dev
git checkout dev

# Сделай любые изменения для теста
# Например, измени что-то в miniapp/app/page.tsx

# Запушь изменения
git add .
git commit -m "Тестовая версия миниапа"
git push origin dev
```

### 7. Получить Preview URL

После пуша:
1. Зайди в Vercel Dashboard
2. Открой проект
3. Увидишь новый деплой из ветки `dev`
4. Используй прод-URL мини-аппа: `https://step-one-app.vercel.app`

## 🎯 ИСПОЛЬЗОВАНИЕ:

### Для тестирования новых фич:

1. Работай в ветке `dev`
2. Делай изменения
3. `git push origin dev`
4. Vercel создаст preview URL
5. Тестируй миниап по preview URL в Telegram

### Для продакшна:

1. Когда все протестировано
2. Мержишь `dev` → `main`: `git checkout main && git merge dev && git push origin main`
3. Vercel автоматически обновит продакшн

## 📝 ПРИМЕРЫ КОМАНД:

```bash
# Переключиться на dev
git checkout dev

# Посмотреть текущую ветку
git branch

# Запустить миниап локально
cd miniapp && npm run dev

# Задеплоить тестовую версию
git add .
git commit -m "Новая фича для теста"
git push origin dev
```

## ⚠️ ВАЖНО:

- **Preview URL** - это твой тестовый миниап, используй его для тестирования
- **Production URL** - это продакшн, обновляется только из `main`
- Всегда тестируй в `dev` перед мержем в `main`!


