# ✅ Деплой успешен! Что дальше:

## 🎯 Настройка тестового окружения:

### 1. Настрой Preview для ветки dev:

В Vercel Dashboard:
1. Settings → Git
2. Production Branch: `main` (уже настроено)
3. Preview Branches: включи `dev` ✅

### 2. Добавь переменные окружения для Preview:

Settings → Environment Variables → Add New

**Выбери окружение: Preview** и добавь:

```
NEXT_PUBLIC_SUPABASE_URL=https://ppisnuivnswwpkoxwpef.supabase.co
SUPABASE_SERVICE_ROLE_KEY=твой-сервисный-ключ-из-supabase
TELEGRAM_BOT_TOKEN=8528023493:AAGzAHYtRXW5OP38AjjrItzH-Idndm4hJ3A
```

**Важно:** Выбери именно **Preview**, чтобы эти переменные использовались только для тестовых деплоев!

### 3. Протестируй Preview:

```bash
cd /Users/eminkupaev/Desktop/step-one-app
git checkout dev
# Сделай любые изменения для теста
git add .
git commit -m "Тестовая фича"
git push origin dev
```

После пуша Vercel создаст Preview URL для тестирования!

### 4. Используй Preview URL:

После пуша в `dev`:
- Зайди в Vercel Dashboard
- Увидишь новый Preview деплой
- Скопируй Preview URL
- Используй его для тестирования в Telegram Mini App

## 🚀 Готово!

Теперь:
- `main` → Production (продакшн)
- `dev` → Preview (тестирование)

Работай в `dev`, тестируй через Preview, мерь в `main` когда готово!
