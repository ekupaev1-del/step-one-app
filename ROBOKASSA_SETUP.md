# Настройка Robokassa - Инструкция

## ✅ Что уже исправлено в коде:

1. **Формат суммы**: Используется `"199.00"` (с точкой, как требует Robokassa)
2. **InvId**: Числовой формат без префикса (например: `1679508122123456789`)
3. **Кодирование**: Правильное URL-кодирование для кириллицы в Description
4. **Подпись**: Правильный формат `MerchantLogin:OutSum:InvId:Password1`
5. **Обработка ошибок**: Улучшенная диагностика и логирование

## 🔧 Что нужно проверить в Vercel:

### 1. Переменные окружения

В Vercel Dashboard → Settings → Environment Variables должны быть:

```
ROBOKASSA_MERCHANT_LOGIN=ваш_логин_мерчанта
ROBOKASSA_PASSWORD1=ваш_пароль_1
ROBOKASSA_PASSWORD2=ваш_пароль_2
```

**ВАЖНО**: 
- Эти переменные должны быть добавлены для **Preview** окружения (ветка `dev`)
- `Password1` используется для создания платежа
- `Password2` используется для проверки результата оплаты

### 2. Проверка переменных

После деплоя откройте:
```
https://ваш-домен.vercel.app/api/robokassa/test
```

Должно показать:
- `hasMerchantLogin: true`
- `hasPassword1: true`
- `hasPassword2: true`
- Тестовый URL для проверки

## 🔍 Проверка в личном кабинете Robokassa:

1. **Войдите в личный кабинет**: https://auth.robokassa.ru/
2. **Проверьте мерчант**:
   - Мерчант должен быть **активен**
   - Нет ограничений на платежи
3. **Проверьте технические настройки**:
   - `Password #1` должен совпадать с `ROBOKASSA_PASSWORD1` в Vercel
   - `Password #2` должен совпадать с `ROBOKASSA_PASSWORD2` в Vercel
4. **Проверьте URL для Result URL**:
   - Должен быть: `https://ваш-домен.vercel.app/api/robokassa/result`
   - Метод: POST или GET (оба поддерживаются)
5. **Проверьте URL для Success URL**:
   - Должен быть: `https://ваш-домен.vercel.app/api/robokassa/success`

## 🧪 Тестирование:

### Вариант 1: Тестовый режим

1. Добавьте в Vercel переменную: `ROBOKASSA_TEST_MODE=true`
2. В тестовом режиме можно использовать тестовые карты
3. После проверки уберите эту переменную

### Вариант 2: Реальный платеж

1. Убедитесь, что все переменные установлены
2. Откройте страницу оплаты в боте
3. Нажмите "Продолжить и оплатить"
4. Должен произойти редирект на страницу Robokassa

## ❌ Если ошибка 500:

1. **Проверьте логи Vercel**:
   - Vercel Dashboard → ваш проект → Logs
   - Найдите логи с `[robokassa/create]`
   - Проверьте, что все параметры правильные

2. **Проверьте учетные данные**:
   - `ROBOKASSA_MERCHANT_LOGIN` должен совпадать с логином в ЛК Robokassa
   - `ROBOKASSA_PASSWORD1` должен совпадать с Password #1 в ЛК Robokassa

3. **Проверьте настройки мерчанта**:
   - Мерчант должен быть активен
   - Нет ограничений на платежи
   - Правильно настроены Result URL и Success URL

4. **Проверьте формат суммы**:
   - В коде используется `"199.00"` (с точкой)
   - Это правильный формат для Robokassa

## 📝 Формат платежа:

- **Сумма**: 199.00 ₽
- **Описание**: "Подписка на сервис питания Step One"
- **Триал**: 3 дня бесплатно после первой оплаты
- **Подписка**: 30 дней после окончания триала

## 🔄 После успешной оплаты:

1. Robokassa отправляет запрос на `/api/robokassa/result`
2. Проверяется подпись с `Password2`
3. Активируется триал на 3 дня
4. Подписка активна 30 дней после триала
5. Пользователь получает доступ к функциям бота

## 📞 Если ничего не помогает:

1. Проверьте логи Vercel - там будут все параметры запроса
2. Проверьте настройки в личном кабинете Robokassa
3. Убедитесь, что переменные окружения установлены для **Preview** окружения
4. Попробуйте тестовый режим (`ROBOKASSA_TEST_MODE=true`)
