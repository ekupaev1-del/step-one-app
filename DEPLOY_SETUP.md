# Настройка автоматического деплоя на Vercel

## ✅ Что уже сделано:

1. ✅ Создан GitHub Actions workflow (`.github/workflows/deploy-vercel.yml`)
   - Автоматически запускается при push в ветки `dev` и `main`
   - Использует Vercel Action для деплоя

2. ✅ Все изменения закоммичены в ветку `dev`

## 🔧 Что нужно настроить:

### 1. Настроить GitHub Secrets

Зайди в GitHub репозиторий: https://github.com/ekupaev1-del/step-one-app/settings/secrets/actions

Добавь следующие secrets:

- `VERCEL_TOKEN` - токен Vercel (получить на https://vercel.com/account/tokens)
- `VERCEL_ORG_ID` - ID организации Vercel (найти в настройках проекта Vercel)
- `VERCEL_PROJECT_ID` - ID проекта Vercel (найти в настройках проекта Vercel)

### 2. Подключить Vercel к GitHub (если еще не подключен)

1. Зайди на https://vercel.com/new
2. Import Git Repository → выбери `ekupaev1-del/step-one-app`
3. Настройки проекта:
   - **Root Directory:** `miniapp`
   - **Framework Preset:** Next.js
4. Deploy

### 3. Настроить переменные окружения в Vercel

В Settings → Environment Variables добавь для **Production** и **Preview**:

- `NEXT_PUBLIC_SUPABASE_URL`
- `SUPABASE_SERVICE_ROLE_KEY`
- `TELEGRAM_BOT_TOKEN`

## 🚀 Как это работает:

1. **При push в `dev` или `main`:**
   - GitHub Actions автоматически запускается
   - Деплоит на Vercel через Vercel Action

2. **Если Vercel подключен к GitHub:**
   - Vercel также автоматически деплоит при push (дублирование, но это нормально)

## 📝 Для push в GitHub:

Если push не работает из-за аутентификации, используй один из способов:

**Вариант 1: Personal Access Token**
```bash
git push https://<TOKEN>@github.com/ekupaev1-del/step-one-app.git dev
```

**Вариант 2: Настроить SSH ключ**
```bash
# Сгенерировать SSH ключ
ssh-keygen -t ed25519 -C "kupaevem@yandex.ru"

# Добавить в GitHub: Settings → SSH and GPG keys → New SSH key
# Скопировать содержимое ~/.ssh/id_ed25519.pub
```

**Вариант 3: Использовать GitHub Desktop или другой GUI клиент**

## ✅ После настройки:

После того как настроишь secrets и сделаешь push, все будет работать автоматически:
- Push в `dev` → автоматический деплой на Vercel Preview
- Push в `main` → автоматический деплой на Vercel Production
