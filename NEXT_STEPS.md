# 🚀 Следующие шаги после миграции

## ✅ Что уже сделано

1. ✅ Структура монорепо создана
2. ✅ Код бота перенесен и адаптирован (1515 строк)
3. ✅ Код миниапа перенесен (12 файлов)
4. ✅ Переменные окружения бота скопированы
5. ✅ Конфиги созданы

## 📋 Что нужно сделать сейчас

### Шаг 1: Переменные окружения миниапа

```bash
cd step-one-app/miniapp
cp .env.example .env.local
# Откройте .env.local и заполните значениями из старого проекта:
# NEXT_PUBLIC_SUPABASE_URL=...
# NEXT_PUBLIC_SUPABASE_ANON_KEY=...
```

### Шаг 2: Установить зависимости

```bash
# Бот
cd step-one-app/bot
npm install

# Миниап
cd ../miniapp
npm install
```

### Шаг 3: Протестировать локально

```bash
# В одном терминале - бот
cd step-one-app/bot
npm run dev

# В другом терминале - миниап
cd step-one-app/miniapp
npm run dev
```

### Шаг 4: Настроить Git

```bash
cd step-one-app
git init
git add .
git commit -m "Initial commit: monorepo with migrated code"

# Создайте репозиторий на GitHub, затем:
git remote add origin https://github.com/YOUR_USERNAME/step-one-app.git
git branch -M main
git push -u origin main

# Создайте ветку dev
git checkout -b dev
git push -u origin dev
```

### Шаг 5: Настроить деплой

Следуйте инструкциям в `DEPLOYMENT.md`:
- Настройте сервер для бота (Ubuntu + PM2)
- Настройте Vercel для миниапа

## ⚠️ Важные замечания

1. **URL миниапа в боте**: После деплоя миниапа на Vercel обновите `MINIAPP_BASE_URL` в `bot/src/index.ts` или добавьте переменную `MINIAPP_BASE_URL` в `bot/.env`

2. **Проверьте импорты**: Убедитесь, что все импорты в миниапе корректны после переноса

3. **Старый проект**: Не удаляйте `nutrition-app` до полной проверки нового проекта

## 📚 Документация

- `QUICKSTART.md` - быстрый старт
- `MIGRATION.md` - детали миграции
- `DEPLOYMENT.md` - полный гайд по деплою
- `STRUCTURE.md` - структура проекта

---

**Готово к использованию!** 🎉
