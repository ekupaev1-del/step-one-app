# Troubleshooting Robokassa Error 29

## Если Error 29 все еще появляется после исправления

### ✅ Что уже исправлено:
- Receipt исключен из подписи (по умолчанию)
- Формат подписи: `MD5(MerchantLogin:OutSum:InvId:Password1:Shp_userId=...)`
- Все переменные добавлены в Vercel

### 🔍 Возможные причины Error 29:

#### 1. Несоответствие Password1 и TEST_MODE (САМАЯ ЧАСТАЯ ПРИЧИНА!)

**Проблема:**
- Если `ROBOKASSA_TEST_MODE=false` (Production), но используется ТЕСТОВЫЙ Password1
- Или наоборот: `ROBOKASSA_TEST_MODE=true`, но используется ПРОДАКШН Password1

**Решение:**
1. Проверьте в Robokassa кабинете:
   - **Настройки** → **Технические настройки**
   - Найдите **Пароль #1** для Production и для Test
   
2. В Vercel убедитесь:
   - Если `ROBOKASSA_TEST_MODE=false` → используйте **ПРОДАКШН Пароль #1**
   - Если `ROBOKASSA_TEST_MODE=true` → используйте **ТЕСТОВЫЙ Пароль #1**

3. Проверьте соответствие:
   ```
   Production:
   ROBOKASSA_TEST_MODE = false
   ROBOKASSA_PASSWORD1 = [ПРОДАКШН Пароль #1 из кабинета]
   
   Preview/Development:
   ROBOKASSA_TEST_MODE = true
   ROBOKASSA_PASSWORD1 = [ТЕСТОВЫЙ Пароль #1 из кабинета]
   ```

#### 2. Переменные не применены (нужен Redeploy)

**Проблема:**
- Переменные добавлены, но старый деплой все еще работает

**Решение:**
1. Vercel Dashboard → **Deployments**
2. Найдите последний деплой
3. Нажмите **три точки (⋯)** → **Redeploy**
4. Дождитесь завершения деплоя
5. Попробуйте снова

#### 3. Неправильный MerchantLogin

**Проблема:**
- MerchantLogin должен быть точно `steopone` (case-sensitive)

**Решение:**
1. Проверьте в Vercel: `ROBOKASSA_MERCHANT_LOGIN = steopone`
2. Убедитесь, что нет лишних пробелов
3. Проверьте через API: `/api/robokassa/debug-signature`

#### 4. Кеш браузера/Telegram WebView

**Проблема:**
- Старая версия закеширована

**Решение:**
1. Закройте Telegram Mini App полностью
2. Откройте заново
3. Или используйте инкогнито режим

#### 5. Используется Preview домен вместо Production

**Проблема:**
- Preview деплой может иметь другие переменные

**Решение:**
1. Убедитесь, что используете Production домен
2. Проверьте в Vercel, что переменные установлены для **Production**

## Пошаговая диагностика

### Шаг 1: Проверьте конфигурацию через API

```bash
curl https://ваш-домен.vercel.app/api/robokassa/debug-signature
```

Должно вернуть:
```json
{
  "ok": true,
  "variant": "without-receipt",
  "merchantLogin": "steopone",
  "isTest": false
}
```

### Шаг 2: Проверьте логи в Vercel

1. Vercel Dashboard → **Deployments** → выберите последний деплой
2. Нажмите **Logs**
3. Найдите строки с `[robokassa]`:
   - `exactSignatureStringMasked: steopone:1.00:InvId:[PASSWORD1]:Shp_userId=...`
   - `signatureVariant: without-receipt`
   - `includeReceiptInSignature: false`

### Шаг 3: Проверьте Password1 в Robokassa

1. Зайдите в Robokassa кабинет
2. **Настройки** → **Технические настройки**
3. Найдите **Пароль #1**:
   - Для Production (если `TEST_MODE=false`)
   - Для Test (если `TEST_MODE=true`)
4. Сравните с `ROBOKASSA_PASSWORD1` в Vercel

### Шаг 4: Проверьте соответствие TEST_MODE и Password1

**Правильная конфигурация:**

```
Production:
ROBOKASSA_TEST_MODE = false
ROBOKASSA_PASSWORD1 = [ПРОДАКШН Пароль #1]
ROBOKASSA_PASSWORD2 = [ПРОДАКШН Пароль #2]

Preview/Development:
ROBOKASSA_TEST_MODE = true
ROBOKASSA_PASSWORD1 = [ТЕСТОВЫЙ Пароль #1]
ROBOKASSA_PASSWORD2 = [ТЕСТОВЫЙ Пароль #2]
```

## Быстрая проверка

Запустите скрипт диагностики:

```bash
node debug-error-29.js
```

Или с кастомным URL:

```bash
node debug-error-29.js https://your-domain.vercel.app
```

## Если ничего не помогает

1. **Проверьте точный формат подписи в логах:**
   - Должно быть: `steopone:1.00:InvId:Password1:Shp_userId=...`
   - НЕ должно быть: `steopone:1.00:InvId:Receipt:Password1:...`

2. **Свяжитесь с поддержкой Robokassa:**
   - Предоставьте им:
     - MerchantLogin
     - OutSum
     - InvId
     - SignatureValue (из логов)
     - Формат подписи (exactSignatureStringMasked)

3. **Проверьте настройки в Robokassa кабинете:**
   - Result URL правильный
   - Success URL правильный
   - Технические настройки соответствуют переменным в Vercel

