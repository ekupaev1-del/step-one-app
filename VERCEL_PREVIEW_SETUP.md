# 🔧 Настройка Preview деплоев в Vercel

## В новых версиях Vercel:

Preview деплои создаются **автоматически** для всех веток, кроме production ветки!

### Что нужно сделать:

1. **Production Branch:**
   - Settings → Git → Production Branch: `main`
   - (Это уже настроено)

2. **Preview деплои:**
   - Vercel автоматически создает Preview для всех остальных веток
   - Включая `dev` ветку!
   - Ничего дополнительно настраивать не нужно

3. **Переменные окружения для Preview:**
   - Settings → Environment Variables
   - При добавлении переменной выбери окружение: **Preview**
   - Добавь:
     - `NEXT_PUBLIC_SUPABASE_URL`
     - `SUPABASE_SERVICE_ROLE_KEY`
     - `TELEGRAM_BOT_TOKEN` = `8528023493:AAGzAHYtRXW5OP38AjjrItzH-Idndm4hJ3A`

4. **Протестируй:**
   ```bash
   git checkout dev
   # сделай изменения
   git push origin dev
   ```
   
   После пуша Vercel автоматически создаст Preview деплой!

## Где найти Preview URL:

После пуша в `dev`:
1. Зайди в Vercel Dashboard → Deployments
2. Увидишь новый деплой из ветки `dev`
3. Для запуска мини-аппа используем прод-URL: `https://step-one-app.vercel.app`

Готово! 🎉
